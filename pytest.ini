[pytest]
testpaths = tests
addopts = -ra
markers =
    governance: governance artifact checks
    archlog: archlogcore topology and content checks
    unit: unit tests
    integration: integration tests
    route: route-matrix tests
    submodule_required: requires initialized git submodules for agent imports
